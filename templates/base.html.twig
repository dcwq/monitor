<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Cronitorex{% endblock %}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
    <style>
        body {
            background-color: #f8f9fa;
        }
        .navbar {
            background-color: #fff;
            box-shadow: 0 1px 3px rgba(0,0,0,.1);
        }
        .card {
            border: none;
            border-radius: 8px;
            box-shadow: 0 2px 4px rgba(0,0,0,.05);
            margin-bottom: 20px;
        }
        .card-header {
            background-color: #fff;
            border-bottom: 1px solid #eee;
            padding: 15px 20px;
        }
        .health-bar {
            height: 6px;
            border-radius: 3px;
            background-color: #eee;
            overflow: hidden;
        }
        .health-bar-fill {
            height: 100%;
            background-color: #4CAF50;
        }
        .health-bar-fail {
            background-color: #F44336;
        }
        .status-badge {
            padding: 5px 10px;
            border-radius: 4px;
            font-size: 12px;
            font-weight: 600;
        }
        .status-healthy {
            background-color: #E8F5E9;
            color: #2E7D32;
        }
        .status-failing {
            background-color: #FFEBEE;
            color: #C62828;
        }
        .execution-chart {
            height: 200px;
            width: 100%;
            background-color: #f9f9f9;
            border-radius: 8px;
            overflow: hidden;
            position: relative;
        }
        .execution-bar {
            position: absolute;
            bottom: 0;
            width: 15px;
            background-color: #673AB7;
            border-radius: 3px 3px 0 0;
        }
        .time-marker {
            position: absolute;
            bottom: -20px;
            font-size: 10px;
            color: #666;
            transform: translateX(-50%);
        }
        .event-marker {
            width: 20px;
            height: 20px;
            border-radius: 50%;
            background-color: #4CAF50;
            display: inline-block;
            margin-right: 5px;
        }
    </style>
    {% block stylesheets %}{% endblock %}
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-light mb-4">
        <div class="container">
            <a class="navbar-brand fw-bold" href="{{ app_url }}/">Cronitorex</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="{{ app_url }}/">Dashboard</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ app_url }}/groups">Groups</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ app_url }}/config/notification-channels">Notifications</a>
                    </li>
                </ul>
                <div class="d-flex">
                    <a href="{{ app_url }}/sync" class="btn btn-sm btn-outline-secondary">
                        <i class="bi bi-arrow-repeat me-1"></i> Sync Logs
                    </a>
                </div>
            </div>
        </div>
    </nav>

    <div class="container">
        {% block content %}{% endblock %}
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    {% block javascripts %}{% endblock %}
</body>
</html>